# Development / CI dependencies — install with: pip install -r requirements-dev.txt
# CI (.github/workflows/ci.yml) installs these individually plus
# opencv-python-headless instead of opencv-contrib-python (no display server).
pytest>=8.0
flake8>=7.0
mypy>=1.8
types-pyserial>=3.5
